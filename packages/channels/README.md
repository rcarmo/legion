# `@legion/channels`

Picoclaw-compatible channel contract and adapters for Telegram long polling and framework-neutral web chat. `LegionChannelRouter` preserves one durable Legion session per channel conversation and correlates replies with inbound message IDs.
